name: 🚀 Deploy to Vercel

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    name: 🔍 Validate Code
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3
    
    - name: 🔎 Kiểm tra MSSV
      run: |
        echo "=== KIỂM TRA MSSV DH52200893 ==="
        if grep -q "DH52200893" index.html; then
          echo "✅ MSSV DH52200893 FOUND IN CODE"
          echo "✅ Student: DH52200893 - Validated"
        else
          echo "❌ MSSV NOT FOUND"
          exit 1
        fi
    
    - name: 📊 Hiển thị thông tin
      run: |
        echo "=== DEPLOYMENT INFO ==="
        echo "Repository: ${{ github.repository }}"
        echo "Branch: ${{ github.ref }}"
        echo "Commit SHA: ${{ github.sha }}"
        echo "Triggered by: ${{ github.actor }}"
        echo "Timestamp: $(date '+%Y-%m-%d %H:%M:%S')"
        echo "MSSV: DH52200893"
        echo "Project: PTPMNM GitHub CI/CD"

  deploy:
    runs-on: ubuntu-latest
    name: 🚀 Deploy to Vercel
    needs: validate
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3
    
    - name: 🚀 Deploy to Vercel
      uses: amondnet/vercel-action@v20
      with:
        vercel-token: ${{ secrets.VERCEL_TOKEN }}
        vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
        vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
        vercel-args: '--prod --confirm'
    
    - name: ✅ Hiển thị kết quả
      run: |
        echo "========================================"
        echo "        🎉 DEPLOYMENT COMPLETED         "
        echo "========================================"
        echo "✅ Code validation: PASSED"
        echo "✅ MSSV check: PASSED (DH52200893)"
        echo "✅ Vercel deployment: SUCCESS"
        echo "👨‍🎓 Student ID: DH52200893"
        echo "📅 Time: $(date '+%Y-%m-%d %H:%M:%S')"
        echo "🔗 URL: Check Vercel Dashboard"
        echo "========================================"